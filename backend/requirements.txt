# Dependencias de runtime da API.
# As libs de reconhecimento facial ficam em requirements-facial.txt (pesam ~1GB).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
python-multipart==0.0.20
# Decodificacao e validacao de imagem antes de chegar ao modelo. Fica no
# requirements base (nao no facial) porque a guarda de upload vale mesmo
# quando a engine em uso e a stub.
pillow==11.1.0
argon2-cffi==23.1.0
# PyJWT em vez de python-jose: mantido ativamente e com superficie bem menor.
pyjwt==2.10.1
# Criptografia em repouso das imagens de rosto (AES-GCM).
cryptography==44.0.0

# Cliente HTTP do envio de push (services/notificacoes.py). Sobe para o
# requirements de runtime porque o agendador roda em producao; ate aqui httpx
# so existia como dependencia de teste.
httpx==0.28.1

# Agendador do lembrete horario, no proprio processo da API. Maquina unica e
# sem fila: um segundo processo no Fly dobraria o custo para um job que roda um
# SELECT a cada poucos minutos.
apscheduler==3.11.0
