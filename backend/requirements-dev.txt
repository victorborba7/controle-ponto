-r requirements.txt

pytest==8.3.4
# 0.26+ pela opcao asyncio_default_test_loop_scope: sem ela, cada modulo teria
# de repetir um pytestmark com loop_scope, e misturar escopos de loop na mesma
# execucao quebra as conexoes asyncpg das fixtures de sessao.
pytest-asyncio==0.26.0
httpx==0.28.1
ruff==0.8.6
